

### 2026-03-12. Reviewed the AutoHarness paper, which demonstrates that LLM agents can eliminate illegal moves by synthesizing constraint-enforcing code harnesses rather than reasoning at runtime. This highlights a critical compile-time vs runtime tradeoff, where smaller models with generated code outperform larger raw models. We discussed whether this pattern applies to Marcus's agent family (Crunch, Grit, Gravel) for managing GitHub API and filesystem boundaries. The conversation suggests potential experiments where LLMs write deterministic policies for constrained environments instead of acting directly. Future work involves evaluating harness generalization and whether this constrains helpful reasoning in open-ended tasks.